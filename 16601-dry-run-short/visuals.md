# Vertical capture plan (9:16)

## Shot 1 — 0:00–0:05
Large text on dark background: **"Before mining: inspect the payload."**
Subtext: `--dry-run --show-payload`

## Shot 2 — 0:05–0:13
Screen capture of the current RustChain miner setup guide with the documented command highlighted. Do not fabricate terminal output.

## Shot 3 — 0:13–0:25
Vertical terminal capture showing only the command being typed:

```bash
cd miners/linux
python3 rustchain_linux_miner.py --dry-run --show-payload
```

If an editor has a real test environment, capture the authentic output from that run. If not, stop the visual after the command entry and use on-screen text: **"Inspect your real output here."**

## Shot 4 — 0:25–0:37
Three stacked callouts:
1. Inspect intended payload
2. Confirm behavior on your machine
3. Decide whether to run live

## Shot 5 — 0:37–0:45
Show the source file path:
`docs/sprint/miner-setup-guide.md`
Then end card: **"Inspect first. Run live second."**

All visuals are exact capture instructions. No fake benchmark, wallet balance, hardware fingerprint, successful submission, or network result is represented.
