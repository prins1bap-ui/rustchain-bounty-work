# Vertical storyboard — 9:16

## 0:00–0:05 — Hook
On-screen text: `ONE CPU = ONE VOTE`
Visual: large CPU icon centered; multiple GPU/hash icons fade into the background.

## 0:05–0:14 — Different consensus input
On-screen text: `RIP-200: hardware identity`
Visual: physical desktop silhouette with callout lines to CPU, board, clock and thermal/fingerprint signals. Do not imply any specific hardware model.

## 0:14–0:24 — Six checks
On-screen text: `6 hardware-level fingerprint checks`
Visual: six neutral check boxes animate around a machine outline. Use the protocol documentation on-screen as a source card rather than inventing check names not shown in the narration.

## 0:24–0:35 — Why fingerprint
On-screen text: `Software labels are cheap. Physical identity is harder to multiply.`
Visual: one physical machine on left; stack of translucent VM windows on right with a question mark. Do not claim VMs are impossible; narration only explains the design goal.

## 0:35–0:45 — Antiquity weighting
On-screen text: `valid hardware → antiquity weighting`
Visual: simple timeline from newer to older hardware, no numeric multipliers.

## 0:45–0:55 — Close
On-screen text: `Hardware identity before hash power`
Visual: RustChain wordmark as plain text plus repo URL `github.com/Scottcjn/Rustchain`.

All visuals can be generated from basic shapes/text. No third-party footage required.
